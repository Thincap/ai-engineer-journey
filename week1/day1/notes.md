# Day 1 Notes

## Overview
- This folder contains `hello_llm.py`.
- The goal is to learn the basics of working with language models and getting a simple prompt response using a shared root virtual environment.

## Key Concepts
- How to organize project files in a workspace
- How to write a simple script that interacts with a language model
- How to use a single root `venv` for the whole repository instead of per-folder environments

## Files
- `hello_llm.py`: example script demonstrating a basic LLM prompt.

## Notes
- `main.py` and `pyproject.toml` were removed from this folder.
- Dependency management now lives at the repository root, and the shared root `venv` is used for all day folders.

## Next Steps
1. Review the code in `hello_llm.py`.
2. Run the script using the repository root virtual environment.
3. Update this note with any new findings or helpful commands.
